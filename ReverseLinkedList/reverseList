// Eeshan Londhe
// Reversing a linkedlist
public void reverseList(Node front) {
   Node current = front;
   Node previous = null;
   while(current != null) { 
      Node cur2 = current.next;
      current.next = previous;
      previous = current;
      current = cur2;
   }
   front = previous;
}
